# Local-only runtime overrides (do not commit real secrets)
models:
  default: "nvidia/nemotron-3-super-120b-a12b:free"
  by_agent_role:
    planner: "nvidia/nemotron-3-super-120b-a12b:free"

reasoner:
  default: "medium"
