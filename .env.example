# Copy to .env and fill real secrets locally (do not commit real values)
OPENROUTER_API_KEY=

# Optional runtime model override (highest priority)
OPENROUTER_DEFAULT_MODEL=nvidia/nemotron-3-super-120b-a12b:free

# Optional local yaml overlay path
# RUNTIME_CONFIG_LOCAL_PATH=./config/runtime.local.yaml
