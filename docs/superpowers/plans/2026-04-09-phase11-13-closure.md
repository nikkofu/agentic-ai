# Phase 11-13 Closure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Finish the remaining planned work from Phase 11-13 by closing the incomplete invalid-output taxonomy and shipping the two missing product-facing gold-path proofs.

**Architecture:** Keep this tranche deliberately small. Do not open new Phase 14 scope. Extend the current runtime contracts only where the existing Phase 12/13 plans are already committed: add real `semantic_tool_loop` classification in runtime core, then prove two additional user-facing end-to-end paths through inspection and delivery truth.

**Tech Stack:** TypeScript, Vitest, existing runtime executor/orchestrator/taskLifecycle/UI inspector stack.

---

## File Structure

### Runtime/Core
- Modify: `src/core/orchestrator.ts`
  - Finish invalid-output taxonomy with `semantic_tool_loop`.
- Modify: `src/runtime/contracts.ts`
  - Keep invalid-output and gold-path contracts aligned if any small type additions are needed.
- Modify: `src/runtime/taskLifecycle.ts`
  - Expose any extra runtime-inspection facts needed for the missing gold-path proofs.

### UI
- Modify: `ui/components/TaskLifecyclePanel.tsx`
  - Surface any additional inspection facts needed by the new proofs.
- Modify: `ui/hooks/eventStreamState.ts`
  - Keep explanation/action text aligned if resume/code-edit proofs introduce new closure semantics.
- Modify: `ui/store/useTaskStore.ts`
  - Keep graph/store explanation language aligned with the new proofs.

### Tests
- Modify: `tests/unit/runtime-invalid-output.test.ts`
- Modify: `tests/unit/orchestrator-autonomous-loop.test.ts`
- Add: `tests/integration/phase13-code-edit-gold-path.test.ts`
- Add: `tests/integration/phase13-async-resume-gold-path.test.ts`
- Modify: `tests/unit/taskLifecycle.test.ts`
- Modify: `tests/unit/ui-event-stream-state.test.ts`
- Modify: `tests/unit/ui-task-store.test.ts`

## Task 1: Complete The Invalid-Output Taxonomy

**Files:**
- Modify: `src/core/orchestrator.ts`
- Modify: `src/runtime/contracts.ts`
- Test: `tests/unit/runtime-invalid-output.test.ts`
- Test: `tests/unit/orchestrator-autonomous-loop.test.ts`

- [ ] **Step 1: Write the failing tests for `semantic_tool_loop`**

Add tests that expect:
- repeated semantically identical tool-call envelopes to classify as `semantic_tool_loop`
- classification to be non-recoverable after the runtime has already issued a repair/retry hint
- orchestrator to block the node with `blocking_reason = "semantic_tool_loop"`

- [ ] **Step 2: Run tests to verify they fail**

Run:
```bash
npm test -- --run tests/unit/runtime-invalid-output.test.ts tests/unit/orchestrator-autonomous-loop.test.ts
```
Expected: FAIL because `semantic_tool_loop` currently exists in types only, not in real runtime classification.

- [ ] **Step 3: Implement minimal runtime classification**

Update orchestrator normalization so:
- semantic repetition of the same tool-call request is recognized
- the runtime emits `InvalidOutputClassified(kind=semantic_tool_loop)`
- the node blocks deterministically instead of drifting into generic exhaustion only

- [ ] **Step 4: Run tests to verify they pass**

Run:
```bash
npm test -- --run tests/unit/runtime-invalid-output.test.ts tests/unit/orchestrator-autonomous-loop.test.ts
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/core/orchestrator.ts src/runtime/contracts.ts tests/unit/runtime-invalid-output.test.ts tests/unit/orchestrator-autonomous-loop.test.ts
git commit -m "feat: classify semantic tool loops explicitly"
```

## Task 2: Add The Missing Code-Edit Gold Path Proof

**Files:**
- Add: `tests/integration/phase13-code-edit-gold-path.test.ts`
- Modify: `src/runtime/taskLifecycle.ts`
- Modify: `ui/components/TaskLifecyclePanel.tsx`
- Test: `tests/unit/taskLifecycle.test.ts`

- [ ] **Step 1: Write the failing integration proof for `code edit + test`**

Add an end-to-end inspection test that proves:
- a code-edit task completes
- a file artifact is archived
- artifact existence/non-empty truth is exposed
- verification evidence is exposed
- inspection explanation/action text is user-readable

- [ ] **Step 2: Run tests to verify they fail**

Run:
```bash
npm test -- --run tests/integration/phase13-code-edit-gold-path.test.ts tests/unit/taskLifecycle.test.ts
```
Expected: FAIL because there is currently no dedicated product proof for this path.

- [ ] **Step 3: Implement the minimal inspection/product surface support**

Only add the runtime-inspection fields needed to support the proof cleanly. Do not redesign the panel.

- [ ] **Step 4: Run tests to verify they pass**

Run:
```bash
npm test -- --run tests/integration/phase13-code-edit-gold-path.test.ts tests/unit/taskLifecycle.test.ts
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/runtime/taskLifecycle.ts ui/components/TaskLifecyclePanel.tsx tests/integration/phase13-code-edit-gold-path.test.ts tests/unit/taskLifecycle.test.ts
git commit -m "test: prove code edit delivery in product inspection"
```

## Task 3: Add The Missing Async-Resume Gold Path Proof

**Files:**
- Add: `tests/integration/phase13-async-resume-gold-path.test.ts`
- Modify: `src/runtime/taskLifecycle.ts`
- Modify: `ui/hooks/eventStreamState.ts`
- Modify: `ui/store/useTaskStore.ts`
- Test: `tests/unit/ui-event-stream-state.test.ts`
- Test: `tests/unit/ui-task-store.test.ts`

- [ ] **Step 1: Write the failing integration proof for `async resume after interruption`**

Add an end-to-end test that proves:
- a task is interrupted / unfinished
- `resumeTask()` completes it
- inspection surfaces async/task closure facts clearly
- explanation/action text is consistent after resume

- [ ] **Step 2: Run tests to verify they fail**

Run:
```bash
npm test -- --run tests/integration/phase13-async-resume-gold-path.test.ts tests/unit/ui-event-stream-state.test.ts tests/unit/ui-task-store.test.ts
```
Expected: FAIL because this product proof does not exist yet.

- [ ] **Step 3: Implement the minimal resume-inspection/UI support**

Only add or normalize the user-facing fields needed by the proof. Keep the graph/banner/panel wording aligned.

- [ ] **Step 4: Run tests to verify they pass**

Run:
```bash
npm test -- --run tests/integration/phase13-async-resume-gold-path.test.ts tests/unit/ui-event-stream-state.test.ts tests/unit/ui-task-store.test.ts
```
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/runtime/taskLifecycle.ts ui/hooks/eventStreamState.ts ui/store/useTaskStore.ts tests/integration/phase13-async-resume-gold-path.test.ts tests/unit/ui-event-stream-state.test.ts tests/unit/ui-task-store.test.ts
git commit -m "test: prove async resume product flow"
```

## Task 4: Closure Verification And Docs Update

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`
- Modify: `docs/phase-handoff-playbook/2026-04-09-phase13-user-productization-handoff.md`

- [ ] **Step 1: Run the closure verification suite**

Run:
```bash
npm test -- --run tests/unit/runtime-invalid-output.test.ts tests/unit/orchestrator-autonomous-loop.test.ts tests/unit/taskLifecycle.test.ts tests/unit/ui-event-stream-state.test.ts tests/unit/ui-task-store.test.ts tests/integration/phase13-gold-path-inspection.test.ts tests/integration/phase13-code-edit-gold-path.test.ts tests/integration/phase13-async-resume-gold-path.test.ts
```
Expected: PASS

- [ ] **Step 2: Run UI type verification**

Run:
```bash
cd ui && npx tsc --noEmit
```
Expected: PASS

- [ ] **Step 3: Update docs**

Document that:
- Phase 12 taxonomy closure now includes `semantic_tool_loop`
- Phase 13 gold-path proofs now cover:
  - research-writing
  - code edit + test
  - async resume after interruption

- [ ] **Step 4: Commit**

```bash
git add README.md CHANGELOG.md docs/phase-handoff-playbook/2026-04-09-phase13-user-productization-handoff.md
git commit -m "docs: close remaining phase 11 to 13 gaps"
```
